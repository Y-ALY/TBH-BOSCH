# ──────────────────────────────────────────────────────────────
# GDPR Fast Filtering Layer – Dependencies
# ──────────────────────────────────────────────────────────────
# Core data modelling & validation
pydantic>=2.7,<3.0

# (Optional) SpaCy NER for unstructured-name detection.
# Uncomment if you want the SpaCy EntityRuler path in addition
# to the pure-regex scanner.
# spacy>=3.7,<4.0

# File ingestion – text extraction from PDFs and Word docs
PyMuPDF>=1.24,<2.0
python-docx>=1.1,<2.0

# Logging colours (dev convenience – zero prod impact)
colorlog>=6.8,<7.0
